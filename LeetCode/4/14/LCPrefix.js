/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function (strs) {}

let strs = ['flower', 'flow', 'flight']

let arr = strs.map((val) => val.split(''))
console.log(arr)


while(strs.length!==0){
  let word = strs.shift()

  console.log(word)
  
}